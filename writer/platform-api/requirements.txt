fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
supabase==2.9.1
python-json-logger==2.0.7
python-multipart==0.0.12
# anthropic/openai must satisfy BOTH apps. Use ranges (not floor pins): the
# vendored Fanout code calls the OpenAI Responses API (client.responses, GPT-5.4
# web_search), which needs openai >= 1.66 — the standalone Fanout service ran the
# latest 1.x, not its declared 1.54 floor. AR Tools' own use (embeddings,
# messages.create) is stable across these ranges.
anthropic>=0.39,<1.0
openai>=1.66,<2.0
pypdf==4.3.1
python-docx==1.1.2
email-validator==2.2.0
google-auth==2.35.0
google-api-python-client==2.149.0

# Topic Fanout Tool (vendored fanout/ sub-package) runtime deps.
# uvicorn/anthropic/openai above were bumped to satisfy both apps.
networkx==3.2.1
python-louvain==0.16
numpy==1.26.4
lingua-language-detector==2.0.2
beautifulsoup4==4.12.3
lxml==5.3.0
spacy==3.7.6
titlecase==2.4.1
tzdata==2024.2
weasyprint==62.3  # HTML/CSS -> PDF for the Client Reporting module (needs the
                  # pango/cairo system libs added in the Dockerfile)
pydyf==0.10.0     # pin: weasyprint 62.3 needs pydyf <0.11 — pydyf 0.11 dropped
                  # the API it calls ("'super' object has no attribute 'transform'")
pillow==10.4.0    # Maps geo-grid: composite the numbered rank pins onto Google's
                  # static-map tile to save a PNG of each scan (services/maps_image.py).
                  # Self-contained wheels (bundled libjpeg/zlib) — no extra apt libs.
