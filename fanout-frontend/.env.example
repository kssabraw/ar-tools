# Topic Fanout frontend env (Vite only exposes VITE_-prefixed vars).
# This app is built as the /fanout section of the AR Tools suite.

# AR Tools platform-api base URL, including the /fanout mount prefix where the
# Topic Fanout routers live (e.g. https://platform-xxxx.up.railway.app/fanout).
VITE_API_BASE_URL=http://localhost:8000/fanout

# Supabase project — MUST match the AR Tools frontend (same project ref) so the
# auth session in localStorage is shared and the team logs in once.
VITE_SUPABASE_URL=https://wvcthtmmcmhkybcesirb.supabase.co
VITE_SUPABASE_ANON_KEY=
